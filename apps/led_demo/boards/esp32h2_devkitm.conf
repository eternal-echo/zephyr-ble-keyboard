# ESP32-H2 riscv32 ilp32 ABI: cbprintf static packaging fails with long double
# Also no MOSFET power gate for LED strip on this board.
CONFIG_CBPRINTF_FP_SUPPORT=n
CONFIG_KEYBOARD_COMMON_LED_CONTROL_POWER_GATE=n

# I2S required for WS2812 via worldsemi,ws2812-i2s
CONFIG_I2S=y